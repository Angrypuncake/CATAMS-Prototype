echo "Running lint-staged (ESLint + Prettier) on staged files..."
if npx lint-staged --cwd project; then
  echo "✅ Code formatting and lint checks passed."
else
  echo "❌ Lint-staged failed. Please fix issues before pushing."
  exit 1
fi