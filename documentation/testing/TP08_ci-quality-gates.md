# TP08 – CI & Quality Gates

- Pre-commit: bad commit message → blocked; lint/format runs.  
- Pre-push: failing Jest → push blocked; passing → push allowed.

**Evidence:** Terminal screenshots of blocked push + passing run.
